import { closeService } from "../services/close.service.js"

export const closeControler = {


    async close(req: Request, res: Response){
        try {
            
        } catch (error) {
            
        }
        
    }
}